name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: relic7663/devops-fabrica-main 

jobs:
  ci:
    name: Build & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./app

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('app/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Build and Test with coverage
        run: chmod +x mvnw && ./mvnw verify --no-transfer-progress

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: app/target/surefire-reports/

  docker:
    name: Docker Build, Scan & Push
    needs: ci
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # CORREGIDO: Se cambió github.repository por env.IMAGE_NAME
      - name: Build Docker image
        run: docker build -t ghcr.io/${{ env.IMAGE_NAME }}:${{ github.sha }} ./app

      # CORREGIDO: Se cambió github.repository por env.IMAGE_NAME
      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}
          format: 'table'
          exit-code: '0'
          severity: 'CRITICAL'

      # CORREGIDO: Se cambió github.repository por env.IMAGE_NAME en los tags y pushes
      - name: Push image to GHCR
        run: |
          docker tag ghcr.io/${{ env.IMAGE_NAMEasd }}:${{ github.sha }} ghcr.io/${{ env.IMAGE_NAME }}:latest
          docker push ghcr.io/${{ env.IMAGE_NAMEasd }}:${{ github.sha }}
          docker push ghcr.io/${{ env.IMAGE_NAMEasd }}:latest






          